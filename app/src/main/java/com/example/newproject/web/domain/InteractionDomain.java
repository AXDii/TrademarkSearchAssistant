package com.example.newproject.web.domain;

import net.sf.json.JSONObject;

import java.util.UUID;

/**
 * 交互Bean，维持在线状态使用
 */
public class InteractionDomain extends BaseDomain<InteractionDomain>{
    //noting,与BaseDomain保持一致即可
}
